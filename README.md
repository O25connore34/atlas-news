# АТЛАС / ATLAS / 经纬

Newsroom React client plus a file-based headless CMS served at `/api`.

```bash
npm install
npm run dev
```

Editions: `/ru`, `/en`, `/zh`. Content lives in `cms/content/`; the Vite plugin exposes REST in `cms/plugin.ts`.
